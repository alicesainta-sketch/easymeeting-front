# FAQ

## 1. 为什么使用事件溯源？
事件溯源让会议状态可回放、可审计，且更容易做异常恢复。

## 2. 状态机的价值是什么？
状态机能把会议生命周期的合法迁移显式化，避免无效状态切换。

## 3. 为什么引擎与 UI 要分离？
引擎保持框架无关、可测试，UI 只关注渲染与交互。

## 4. 事件量太大怎么办？
当前采用容量上限控制，后续可引入分页/归档策略或接入后端。

## 5. 如何扩展新的事件类型？
在 `protocol.js` 增加事件与 payload 约束，并更新 formatters/selectors。
