# Ops Checklist

## 发布前检查
- `npm run test` 通过
- `npm run lint` 通过
- 核心会议流程手动检查（列表/详情/入会/回放）
- 关键文档（changelog/decisions）已更新

## 风险评估
- 是否影响会议生命周期状态机
- 是否影响事件写入/回放逻辑
- 是否引入新的依赖或数据结构变更

## 回滚步骤
1. 回退到上一个稳定 tag。
2. 恢复上一版本构建产物。
3. 验证关键路径与会议流程。
